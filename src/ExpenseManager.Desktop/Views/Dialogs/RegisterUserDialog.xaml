<Window x:Class="ExpenseManager.Desktop.Views.Dialogs.RegisterUserDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:ExpenseManager.Desktop.ViewModels.Dialogs"
    xmlns:extensions="clr-namespace:ExpenseManager.Desktop.Extensions"
    mc:Ignorable="d"
    Title="{extensions:TranslateExtension REGISTER_DIALOG_TITLE}"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
    ResizeMode="NoResize"
    Background="{DynamicResource CardBrush}"
    Foreground="{DynamicResource PrimaryTextBrush}"
        FontFamily="Segoe UI"
        MinWidth="360"
        MinHeight="320">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,4,0,12" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="Background" Value="#111821" />
            <Setter Property="BorderBrush" Value="#222C37" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="CaretBrush" Value="{DynamicResource AccentBrush}" />
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Margin" Value="0,4,0,12" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="Background" Value="#111821" />
            <Setter Property="BorderBrush" Value="#222C37" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="CaretBrush" Value="{DynamicResource AccentBrush}" />
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="{extensions:TranslateExtension REGISTER_DIALOG_HEADER}" FontSize="18" FontWeight="SemiBold" />
            <TextBlock Text="{extensions:TranslateExtension REGISTER_DIALOG_SUBTITLE}" Style="{StaticResource MutedText}" />
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,16,0,0">
            <TextBlock Text="{extensions:TranslateExtension LABEL_USERNAME}" FontWeight="SemiBold" />
            <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="{extensions:TranslateExtension LABEL_DISPLAY_NAME}" FontWeight="SemiBold" />
            <TextBox Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="{extensions:TranslateExtension LABEL_PASSWORD}" FontWeight="SemiBold" />
            <PasswordBox PasswordChanged="OnPasswordChanged" />

            <TextBlock Text="{extensions:TranslateExtension LABEL_CONFIRM_PASSWORD}" FontWeight="SemiBold" />
            <PasswordBox PasswordChanged="OnConfirmPasswordChanged" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
            <Button Content="{extensions:TranslateExtension ACTION_CANCEL}" Width="120" Style="{StaticResource SidebarLinkButton}" Margin="0,0,12,0" Click="OnCancelClicked" />
            <Button Content="{extensions:TranslateExtension ACTION_SAVE}" Width="140" Style="{StaticResource AccentButton}" IsDefault="True" Click="OnSaveClicked" />
        </StackPanel>
    </Grid>
</Window>
