<Window x:Class="ExpenseManager.Desktop.Views.Dialogs.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:extensions="clr-namespace:ExpenseManager.Desktop.Extensions"
    mc:Ignorable="d"
    Title="{extensions:TranslateExtension TITLE_AUTHENTICATION}"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
    ResizeMode="NoResize"
    Background="{DynamicResource CardBrush}"
    Foreground="{DynamicResource PrimaryTextBrush}"
        FontFamily="Segoe UI"
        MinWidth="360"
        MinHeight="260">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="{extensions:TranslateExtension LOGIN_HEADER_SIGN_IN}" FontSize="20" FontWeight="SemiBold" />
            <TextBlock Text="{extensions:TranslateExtension LOGIN_SUBTITLE}" Style="{StaticResource MutedText}" />
        </StackPanel>

        <StackPanel Grid.Row="1">
            <TextBlock Text="{extensions:TranslateExtension LABEL_USERNAME}" FontWeight="SemiBold" />
            <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,16" />
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Text="{extensions:TranslateExtension LABEL_PASSWORD}" FontWeight="SemiBold" />
            <PasswordBox Margin="0,4,0,16" PasswordChanged="OnPasswordChanged" />
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{extensions:TranslateExtension ACTION_CREATE_ACCOUNT}" Width="140" Style="{StaticResource SidebarLinkButton}" Margin="0,0,12,0" Click="OnRegisterClicked" />
            <Button Content="{extensions:TranslateExtension ACTION_CANCEL}" Width="120" Style="{StaticResource SidebarLinkButton}" Margin="0,0,8,0" Command="{Binding CancelCommand}" />
            <Button Content="{extensions:TranslateExtension ACTION_SIGN_IN}" Width="140" Style="{StaticResource AccentButton}" Command="{Binding LoginCommand}" />
        </StackPanel>
    </Grid>
</Window>
